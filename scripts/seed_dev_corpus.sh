#!/usr/bin/env bash
# Populate the local index with the bundled test fixture corpus so retrieval
# has something to return out of the box. Implementation lands in T050 (US2).
set -euo pipefail

echo "[rag-nano] seed_dev_corpus.sh — placeholder; wired in T050 (US2 ingest)."
